{% extends 'UNAMAppBundle::layout.html.twig' %}

{% block navegacion %}
	<ol class="breadcrumb">
		<li><a href="{{path('homepage')}}"><svg class="glyph stroked home"><use xlink:href="#stroked-home"></use></svg></a></li>
		<li><a href="{{path('calificaciones')}}"><svg class="glyph stroked clipboard with paper"><use xlink:href="#stroked-clipboard-with-paper"/></svg></a> Cursos </li>
	</ol>
{% endblock %}


{% block content %}
    
    <div class="col-lg-12">
        <div class="page-header ">
            <h2 class="">Calificaciones</h2>
        </div>  
    
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>Fecha Ingreso</th>
                        <th>Status</th>
                        <th>Tipo</th>
                        <th>Grupo</th>
                        <th>Alumno</th>
                        <th>Calificacion</th>
                        <th>Acciones</th>                       
                    </tr>
                </thead>
                <tbody>
                {% for entity in entities %}
                    <tr>
                        <td>{{ entity.fechaIngreso | date('d/m/Y') }}</td>
                        <td>{{ entity.statusString }}</td>
                        <td>{{ entity.tipoString }}</td>
                        <td>{{ entity.grupo }}</td>
                        <td>{{ entity.alumno }}</td>
                        <td>{{ entity.calificacion }}</td>
                        <td>
                           <a class="btn btn-primary" href="{{ path('calificaciones_show', { 'id': entity.id }) }}">Ver</a>                               
                           <a class="btn btn-info" href="{{ path('calificaciones_edit', { 'id': entity.id }) }}">Editar</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
                
            </table>
        </div>

    <table class="records_list">
        
       
    </table>

       
        
            <a class="btn btn-success" href="{{ path('calificaciones_new') }}">
                Agregar Nuevo
            </a>
        
    
                
     </div>           
    {% endblock %}
