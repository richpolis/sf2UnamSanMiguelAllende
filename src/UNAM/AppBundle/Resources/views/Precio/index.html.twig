{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Precio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Precio</th>
                <th>Fechainicio</th>
                <th>Fechafinal</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('precios_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.precio }}</td>
                <td>{% if entity.fechaInicio %}{{ entity.fechaInicio|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.fechaFinal %}{{ entity.fechaFinal|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('precios_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('precios_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('precios_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
