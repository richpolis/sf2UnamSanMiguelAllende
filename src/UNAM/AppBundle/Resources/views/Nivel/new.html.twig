{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Nivel creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('niveles') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
